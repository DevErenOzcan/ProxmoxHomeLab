---
all:
  children:
    proxmox_nodes:
      hosts:
        pve1:
          ansible_host: 192.168.1.200
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
    ubuntu_desktop:
      hosts:
        desktop-vm:
          ansible_host: 10.10.10.11
          ansible_user: ubuntu
    opnsense:
      hosts:
        firewall:
          ansible_host: 10.10.10.1  # Assuming this is the OPNsense IP
          ansible_user: root
